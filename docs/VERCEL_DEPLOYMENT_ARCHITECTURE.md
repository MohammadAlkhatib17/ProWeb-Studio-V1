# Vercel Deployment Architecture & Flow

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                         GITHUB REPOSITORY                            │
│                  contact-prowebstudio/ProWeb-Studio                  │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                   ┌─────────┴─────────┐
                   │   git push main   │
                   └─────────┬─────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
        ▼                    ▼                    ▼
┌───────────────┐    ┌──────────────┐    ┌──────────────┐
│  GitHub       │    │   Vercel     │    │  Playwright  │
│  Actions CI   │    │   Platform   │    │  E2E Tests   │
└───────────────┘    └──────────────┘    └──────────────┘
```

---

## Environment Validation Decision Flow

```
┌─────────────────────────────────────────────────────────────┐
│              ENVIRONMENT VALIDATION LOGIC                    │
└─────────────────────────────────────────────────────────────┘

Start Build Process
      │
      ▼
┌──────────────────┐
│ Check Platform   │
└────────┬─────────┘
         │
    ┌────┴────┐
    │ VERCEL? │
    └────┬────┘
         │
    ┌────┴────────────────────────┐
    │                             │
   YES                           NO
    │                             │
    ▼                             ▼
┌─────────────────┐      ┌──────────────────┐
│ ✅ SKIP         │      │ Check Override   │
│ Validation      │      └────────┬─────────┘
│                 │               │
│ Reason:         │          ┌────┴────┐
│ VERCEL=1 set    │          │ SKIP?   │
│ Platform manages│          └────┬────┘
│ env vars        │               │
└─────────────────┘          ┌────┴──────────────┐
                             │                   │
                            YES                 NO
                             │                   │
                             ▼                   ▼
                    ┌─────────────────┐  ┌──────────────┐
                    │ ✅ SKIP         │  │ Check Mode   │
                    │ Validation      │  └──────┬───────┘
                    │                 │         │
                    │ Reason:         │    ┌────┴────┐
                    │ SKIP_ENV_       │    │  PROD?  │
                    │ VALIDATION=true │    └────┬────┘
                    └─────────────────┘         │
                                           ┌────┴─────────┐
                                           │              │
                                          YES            NO
                                           │              │
                                           ▼              ▼
                                   ┌──────────────┐  ┌─────────┐
                                   │ 🔍 RUN       │  │ ✅ SKIP │
                                   │ VALIDATION   │  │ Dev Mode│
                                   └──────┬───────┘  └─────────┘
                                          │
                                     ┌────┴────┐
                                     │ Result? │
                                     └────┬────┘
                                          │
                                    ┌─────┴─────┐
                                    │           │
                                  PASS        FAIL
                                    │           │
                                    ▼           ▼
                            ┌──────────┐  ┌──────────┐
                            │ ✅ BUILD │  │ ❌ BUILD │
                            │ SUCCEEDS │  │ FAILS    │
                            └──────────┘  └──────────┘
```

---

## CI/CD Pipeline Flow

```
┌────────────────────────────────────────────────────────────────────┐
│                   GITHUB ACTIONS WORKFLOW                           │
└────────────────────────────────────────────────────────────────────┘

Push to main/develop
      │
      ▼
┌──────────────────────────────────────────────────────────────────┐
│                      QUALITY CHECKS JOB                           │
│                                                                   │
│  1. Checkout code                                                 │
│  2. Setup Node.js 20                                              │
│  3. npm ci                                                        │
│  4. npm run lint          ────────────► ✅ PASS                   │
│  5. npm run type-check    ────────────► ✅ PASS                   │
└──────────────────────────┬───────────────────────────────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
        ▼                  ▼                  ▼
┌───────────────┐  ┌──────────────┐  ┌──────────────┐
│ BUNDLE SIZE   │  │  E2E TESTS   │  │ LIGHTHOUSE   │
│ CHECK JOB     │  │     JOB      │  │   CI JOB     │
└───────────────┘  └──────────────┘  └──────────────┘
        │                  │                  │
        ▼                  ▼                  ▼

┌───────────────────────────────────────────────────────────────┐
│ BUNDLE SIZE CHECK                                              │
│                                                                │
│ 1. npm ci                                                      │
│ 2. npm run build                                               │
│    ENV: SKIP_ENV_VALIDATION=true  ──────► ✅ Validation Skipped│
│    ENV: NODE_ENV=production                                    │
│ 3. Bundle analysis         ──────────────► ✅ PASS             │
└───────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────┐
│ E2E TESTS (only on push)                                       │
│                                                                │
│ 1. npm ci                                                      │
│ 2. Install Playwright browsers                                 │
│ 3. Wait for Vercel deployment  ──────────► ⏳ 300s timeout    │
│    - Uses patrickedqvist/wait-for-vercel-preview               │
│    - Gets deployment URL                                       │
│ 4. Run Playwright tests                                        │
│    ENV: PLAYWRIGHT_TEST_BASE_URL=<vercel-url>                  │
│    - Tests run on deployed site  ─────────► ✅ PASS           │
│ 5. Upload test results                                         │
└───────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────┐
│ LIGHTHOUSE CI (only on push)                                   │
│                                                                │
│ 1. Wait for Vercel deployment  ──────────► ⏳ 300s timeout    │
│ 2. Run Lighthouse CI on deployed URL                           │
│    - Performance metrics collected  ──────► ✅ PASS           │
│ 3. Post results to GitHub                                      │
└───────────────────────────────────────────────────────────────┘
```

---

## Vercel Build Process

```
┌────────────────────────────────────────────────────────────────┐
│                    VERCEL BUILD PIPELINE                        │
└────────────────────────────────────────────────────────────────┘

GitHub Push
      │
      ▼
┌────────────────────┐
│ Vercel Detected    │
│ New Commit         │
└─────────┬──────────┘
          │
          ▼
┌────────────────────────────────────────────────────────────────┐
│ 1. ENVIRONMENT SETUP                                            │
│    - Set VERCEL=1                                               │
│    - Set VERCEL_URL=pro-web-studio-abc123.vercel.app           │
│    - Set VERCEL_ENV=production|preview|development              │
│    - Inject env vars from dashboard:                            │
│      • SITE_URL                                                 │
│      • NEXT_PUBLIC_PLAUSIBLE_DOMAIN                             │
│      • CONTACT_INBOX                                            │
│      • NEXT_PUBLIC_RECAPTCHA_SITE_KEY                           │
│      • RECAPTCHA_SECRET_KEY                                     │
└─────────────────────────────┬──────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────────┐
│ 2. INSTALL DEPENDENCIES                                         │
│    $ npm ci                                                     │
│    ✅ Dependencies installed                                    │
└─────────────────────────────┬──────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────────┐
│ 3. BUILD APPLICATION                                            │
│    $ npm run build                                              │
│                                                                 │
│    ┌─────────────────────────────────────────┐                 │
│    │ a. Run next.config.mjs                  │                 │
│    │    - Check VERCEL=1? YES                │                 │
│    │    - ✅ Skip external validation script │                 │
│    │    - ✅ Skip inline validation          │                 │
│    └─────────────────────────────────────────┘                 │
│                                                                 │
│    ┌─────────────────────────────────────────┐                 │
│    │ b. Next.js Build                        │                 │
│    │    - Compile app directory              │                 │
│    │    - Optimize images                    │                 │
│    │    - Generate static pages              │                 │
│    │    - Bundle JavaScript                  │                 │
│    │    ✅ Build successful                  │                 │
│    └─────────────────────────────────────────┘                 │
└─────────────────────────────┬──────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────────┐
│ 4. DEPLOYMENT                                                   │
│    - Upload build to Vercel CDN                                 │
│    - Deploy to regions: cdg1, lhr1, fra1                        │
│    - Configure headers from vercel.json                         │
│    - Set up routes and redirects                                │
│    ✅ Deployment complete                                       │
└─────────────────────────────┬──────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────────┐
│ 5. POST-DEPLOYMENT                                              │
│    - Generate deployment URL                                    │
│    - Post URL to GitHub PR (if preview)                         │
│    - Trigger GitHub Actions E2E tests                           │
│    - Update deployment status: Ready ✅                         │
└────────────────────────────────────────────────────────────────┘
```

---

## Environment Variable Flow

```
┌──────────────────────────────────────────────────────────────────┐
│              ENVIRONMENT VARIABLE SOURCES & FLOW                  │
└──────────────────────────────────────────────────────────────────┘

LOCAL DEVELOPMENT
┌─────────────────┐
│ .env.local      │
│ .env.development│  ────────► process.env ────────► Next.js App
│ .env            │
└─────────────────┘

GITHUB ACTIONS CI
┌─────────────────┐
│ GitHub Secrets  │
│ Workflow env:   │  ────────► process.env ────────► Build Process
│   SKIP_ENV_     │                                   (validation 
│   VALIDATION    │                                    skipped)
└─────────────────┘

VERCEL DEPLOYMENT
┌─────────────────────────────────┐
│ Vercel Dashboard                │
│ Project Settings → Env Vars:    │
│                                 │
│ Production:                     │
│   SITE_URL                      │
│   NEXT_PUBLIC_PLAUSIBLE_DOMAIN  │
│   CONTACT_INBOX                 │
│   NEXT_PUBLIC_RECAPTCHA_SITE_KEY│
│   RECAPTCHA_SECRET_KEY          │
│                                 │
│ Auto-injected:                  │
│   VERCEL=1                      │  ────────► Next.js Build
│   VERCEL_URL                    │            (validation 
│   VERCEL_ENV                    │             auto-skipped)
└─────────────────────────────────┘

PLAYWRIGHT TESTS
┌─────────────────────────────────┐
│ From GitHub Actions:            │
│   PLAYWRIGHT_TEST_BASE_URL      │  ────────► Playwright Config
│   (set from Vercel deploy URL)  │            → getBaseURL()
└─────────────────────────────────┘
```

---

## Security Layers

```
┌──────────────────────────────────────────────────────────────────┐
│                      SECURITY ARCHITECTURE                        │
└──────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ Layer 1: Build-Time Validation                                  │
│ ──────────────────────────────────────────────────────────      │
│ • Validates critical env vars before build                      │
│ • Rejects placeholder values                                    │
│ • Prevents deployments with missing config                      │
│ • Skipped on Vercel (platform handles validation)               │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ Layer 2: Platform Security                                       │
│ ──────────────────────────────────────────────────────────────  │
│ • Vercel encrypts environment variables                         │
│ • Variables never exposed in logs (values hidden)               │
│ • GitHub Secrets encrypted at rest                              │
│ • HTTPS enforced for all deployments                            │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ Layer 3: Runtime Security                                        │
│ ──────────────────────────────────────────────────────────────  │
│ • CSP headers prevent XSS attacks                               │
│ • HSTS enforces HTTPS                                           │
│ • X-Frame-Options prevents clickjacking                         │
│ • reCAPTCHA protects forms from bots                            │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ Layer 4: Access Control                                          │
│ ──────────────────────────────────────────────────────────────  │
│ • Vercel dashboard requires authentication                      │
│ • GitHub repository access controls                             │
│ • Environment-specific variables (prod/preview/dev)             │
│ • Audit logs for all changes                                    │
└─────────────────────────────────────────────────────────────────┘
```

---

## Deployment Timeline

```
┌──────────────────────────────────────────────────────────────────┐
│                  TYPICAL DEPLOYMENT TIMELINE                      │
└──────────────────────────────────────────────────────────────────┘

T+0:00  │ git push origin main
        │
T+0:05  │ ┌─ Vercel detects push
        │ └─ GitHub Actions starts CI
        │
T+0:10  │ ┌─ Vercel: npm ci (install dependencies)
        │ └─ GitHub: quality-checks job starts
        │
T+0:30  │ ┌─ Vercel: npm run build
        │ │  ├─ Validation skipped (VERCEL=1)
        │ │  └─ Next.js build starts
        │ └─ GitHub: quality-checks complete ✅
        │
T+1:00  │ ┌─ Vercel: Build complete
        │ └─ GitHub: bundle-size-check starts
        │
T+1:30  │ ┌─ Vercel: Deployment to CDN
        │ └─ GitHub: bundle-size-check complete ✅
        │
T+2:00  │ ┌─ Vercel: Deployment ready ✅
        │ │  URL: https://pro-web-studio-abc123.vercel.app
        │ └─ GitHub: e2e-tests starts (waits for Vercel)
        │
T+2:30  │ ┌─ GitHub: e2e-tests finds deployment
        │ └─ GitHub: Playwright tests run on deployed URL
        │
T+3:00  │ ┌─ GitHub: e2e-tests complete ✅
        │ └─ GitHub: lighthouse-ci starts
        │
T+4:00  │ └─ GitHub: lighthouse-ci complete ✅
        │
T+4:00  │ ✅ ALL CHECKS PASSED
        │ 🎉 DEPLOYMENT SUCCESSFUL

Total Time: ~4-5 minutes
```

---

## File Interaction Map

```
┌──────────────────────────────────────────────────────────────────┐
│               FILE DEPENDENCIES & INTERACTIONS                    │
└──────────────────────────────────────────────────────────────────┘

.env.example ──────────────► Developer Reference
                             (copy to .env.local)

.env.local ────────────────► Local Development Only
                             (gitignored, not committed)

site/src/lib/env.required.mjs ─┬──► scripts/validate-production-env.js
                               ├──► site/scripts/validate-env.js
                               └──► site/next.config.mjs

scripts/validate-production-env.js ──► Called by CI/CD pipelines
                                       (or manually for testing)

site/scripts/validate-env.js ──────► Called by package.json build:prod
                                      (skipped on Vercel)

site/next.config.mjs ───────────────► Runs during next build
  ├─ Calls validate-production-env.js
  └─ Runs inline validateProductionEnv()

site/playwright.config.ts ──────────► Used by Playwright tests
  └─ getBaseURL() checks VERCEL_URL

.github/workflows/ci.yml ───────────► GitHub Actions workflow
  ├─ Sets SKIP_ENV_VALIDATION=true
  ├─ Waits for Vercel deployment
  └─ Runs tests on deployed URL

site/vercel.json ───────────────────► Vercel configuration
  ├─ Defines build command: npm run build
  ├─ Defines regions: Europe
  └─ Sets headers and caching rules

Vercel Dashboard Env Vars ─────────► Injected at build time
  └─ Only available on Vercel platform
```

---

## Error Handling Flow

```
┌──────────────────────────────────────────────────────────────────┐
│                    ERROR HANDLING STRATEGY                        │
└──────────────────────────────────────────────────────────────────┘

Build Error Detected
        │
        ▼
┌───────────────────┐
│ Where did it fail?│
└───────┬───────────┘
        │
    ┌───┴───────────────────────────┐
    │                               │
    ▼                               ▼
┌─────────────┐              ┌──────────────┐
│   Vercel    │              │ GitHub Actions│
└──────┬──────┘              └──────┬───────┘
       │                            │
       ▼                            ▼
┌──────────────────┐        ┌─────────────────┐
│ Check Build Logs │        │ Check Job Logs  │
│ in Dashboard     │        │ in Actions      │
└──────┬───────────┘        └──────┬──────────┘
       │                           │
       ▼                           ▼
┌──────────────────────────────────────────────┐
│ Common Error Types:                          │
│                                              │
│ 1. Missing Environment Variable              │
│    → Add to Vercel dashboard                 │
│    → Redeploy                                │
│                                              │
│ 2. Invalid Environment Value                 │
│    → Check value format (URL, email, etc.)   │
│    → Update in dashboard                     │
│    → Redeploy                                │
│                                              │
│ 3. Build Timeout                             │
│    → Check for infinite loops                │
│    → Optimize build process                  │
│    → Contact Vercel support                  │
│                                              │
│ 4. E2E Test Failure                          │
│    → Check if site loads correctly           │
│    → Verify VERCEL_URL is set               │
│    → Check Playwright timeout settings       │
│                                              │
│ 5. Lighthouse CI Failure                     │
│    → Check performance metrics               │
│    → May be acceptable if slight degradation │
│    → Review budget thresholds                │
└──────────────────────────────────────────────┘
```

---

**Documentation Version**: 1.0.0  
**Last Updated**: November 29, 2025  
**Status**: Production Ready ✅
