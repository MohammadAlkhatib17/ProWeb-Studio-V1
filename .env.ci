# CI Environment Configuration
# Minimal valid values for CI/CD pipeline testing
# These are NOT production secrets - safe for CI logs

# Site Configuration
SITE_URL=https://ci-build.local
SITE_NAME="CI Test Build"

# Analytics (valid domain format)
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=ci-build.local

# Contact (valid email format)
CONTACT_INBOX=ci-noreply@ci-build.local

# reCAPTCHA (test keys from Google - safe for CI)
# These are official Google test keys that always pass validation
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=6LeIxAcTAAAAAJcZVRqyHh71UMIEGNQ_MXjiZKhI
RECAPTCHA_SECRET_KEY=6LeIxAcTAAAAAGG-vFI1TnRWxMZNFuojJ4WifJWe

# Optional - disabled for CI
BREVO_SMTP_USER=
BREVO_SMTP_PASS=
BREVO_API_KEY=
BREVO_LIST_ID=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
NEXT_PUBLIC_ENABLE_WEB_VITALS=false
