/**
 * Metadata Components
 * 
 * Components for injecting structured data and metadata into pages
 */

export { StructuredData } from './StructuredData';
export { PageStructuredData } from './PageStructuredData';
