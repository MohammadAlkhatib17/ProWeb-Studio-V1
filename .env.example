# ============================================================================
# ProWeb-Studio Environment Variables
# ============================================================================
# Copy this file to .env.local for local development
# For production deployment, set these in your platform's dashboard:
#   - Vercel: Project Settings → Environment Variables
#   - Netlify: Site Settings → Environment Variables
#   - Other: Your platform's equivalent
# ============================================================================

# ----------------------------------------------------------------------------
# SITE CONFIGURATION
# ----------------------------------------------------------------------------
# Your production domain (no trailing slash, HTTPS required for production)
SITE_URL=https://yoursite.com

# ----------------------------------------------------------------------------
# ANALYTICS
# ----------------------------------------------------------------------------
# Domain registered in Plausible Analytics (no protocol, no www)
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=yoursite.com

# ----------------------------------------------------------------------------
# CONTACT FORM
# ----------------------------------------------------------------------------
# Email address to receive contact form submissions
CONTACT_INBOX=contact@yoursite.com

# ----------------------------------------------------------------------------
# GOOGLE reCAPTCHA v2
# ----------------------------------------------------------------------------
# Get keys from: https://www.google.com/recaptcha/admin
# Public site key (visible in client-side code)
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=6LeIxAcTAAAAAJcZVRqyHh71UMIEGNQ_MXjiZKhI

# Secret key (server-side only, keep secure!)
RECAPTCHA_SECRET_KEY=6LeIxAcTAAAAAGG-vFI1TnRWxMZNFuojJ4WifJWe

# ----------------------------------------------------------------------------
# VERCEL ENVIRONMENT (Auto-injected by platform - DO NOT SET MANUALLY)
# ----------------------------------------------------------------------------
# VERCEL=1                          # Indicates running on Vercel
# VERCEL_URL=your-project.vercel.app  # Auto-generated preview URL
# VERCEL_ENV=production|preview|development  # Deployment environment

# ----------------------------------------------------------------------------
# CI/CD OVERRIDES
# ----------------------------------------------------------------------------
# Set to 'true' in CI pipelines to skip environment validation
# Used by GitHub Actions, GitLab CI, etc.
# SKIP_ENV_VALIDATION=true

# ----------------------------------------------------------------------------
# NOTES
# ----------------------------------------------------------------------------
# 1. NEVER commit .env.local or .env files to version control
# 2. Test keys above are Google's official test keys for development
# 3. Replace test keys with real keys in production
# 4. Environment validation automatically skips on Vercel (detects VERCEL=1)
# 5. For local production builds: NODE_ENV=production npm run build
# 6. See docs/VERCEL_DEPLOYMENT_GUIDE.md for complete deployment instructions

